/**
 * Compatibility entrypoint for the existing storefront.
 *
 * The source of truth now lives in app/data so the public website and the
 * administration area can consume data without coupling it to UI code.
 */
export * from "../data/site-data";

